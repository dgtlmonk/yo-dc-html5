language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-dc-html5' ]; then cd .. && eval "mv $currentfolder generator-dc-html5" && cd generator-dc-html5; fi

